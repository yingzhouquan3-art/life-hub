# 我的生活中枢 · 使用指南

> 一个只供自己使用的一体化生活平台。当前包含生活总览、生活轨迹、生活日历、个人目标、个人账本、健身、身体、饮食、恢复、学习、个人节奏与日记复盘。
> 个人账本继续诚实区分家庭支持与自主所得。

---

## 0. 这是什么 / 为什么用它

打开平台后，左侧模块栏提供这些入口：

- **总览**：查看今天的账本、运动与饮食状态，以及下一步提示
- **轨迹**：不用输入关键词，按时间连续回看各模块记录，并可按来源、类型和日期筛选
- **日历**：按日期统一查看所有模块的已发生记录、待办与固定账单安排
- **目标**：记录长期生活方向、进行/暂停/完成状态以及阶段里程碑
- **账本**：使用原有完整个人财务功能
- **健身**：记录活动、时长、强度和备注
- **饮食**：记录餐次、饮水以及可选的营养数值
- **恢复**：每天记录一次睡眠与主观状态；同一天再次保存会更新原记录
- **学习**：记录完成的科目、专注时长和主观专注度
- **节奏**：管理今日待办与每日习惯；逾期只表示日期已过，习惯归档会保留历史打卡
- **复盘**：每日写一条可更新的个人回顾，并查看本周各模块已记录事实的汇总

“复盘”中的亮点、困难、感谢和自由记录任选一项即可保存。同一天再次保存会更新原记录，不会生成重复日记。每周快照按周一至周日汇总账本、运动、饮食、恢复、学习、待办、习惯和回顾；其中 0 只表示没有记录，空白平均值表示未知，平台不会据此评价你的表现。

“日历”把各模块的资料按日期聚合，但不会创建另一份副本。圆点表示已经发生并保存的生活事实，空心标记表示待办或固定账单等日期安排；安排即使已经完成，仍保持“安排”的身份，避免把计划和记录混在一起。点击某条内容可以前往它的来源模块。

“轨迹”适合在不知道该搜什么时回看生活。它按日期从新到旧排列账目、运动、身体、饮食、恢复、学习、习惯打卡、复盘与目标等条目；可以筛选来源模块、已经发生/生活安排/长期条目和日期范围。点击条目会进入来源模块，轨迹页本身不会修改数据。

“目标”适合“完成本学期课程项目”“跑完第一次 5 公里”这类需要一段时间靠近的方向。可以设置可选目标日期、写下原因，并拆成若干里程碑。全部里程碑完成后，平台不会自动宣布目标完成；进行、暂停或完成状态始终由你主动选择。带金额的储蓄目标继续放在个人账本中，两类目标不会混算。

页面右上角的“搜索”可以在任何模块打开，也可以按 `Ctrl+K`。输入关键词后，可按来源模块和日期范围筛选账本、健身、身体、饮食、恢复、学习、节奏、复盘和目标。搜索结果只显示定位摘要；点击结果会进入来源模块，真正的编辑、状态调整或删除仍在来源模块中完成。搜索行为不会写入数据库。

下面的财务说明对应“个人账本”模块。

普通记账 App 只回答**「我今天花了多少」**。
本系统同时回答三个问题：

1. 我现在可支配的钱还能支持多少天生活？
2. 其中有多少来自家庭支持，有多少是我自己获得的？
3. 我的自主收入已经覆盖了多少日常支出？

- **蓝色方格**：家庭生活费与起始可支配存款形成的生活续航
- **金色方格**：奖学金、兼职、实习、个人项目、投资等自主所得形成的续航
- **深灰方格**：当前资金尚不能覆盖的未来日子

它不是用生活费制造“假装财富自由”的数字，而是一面属于你的成长进度灯。

核心公式：

```
avg_daily_expense = total_expense / tracking_days
current_balance   = initial_assets + family_support + independent_income - total_expense
runway_days       = floor(current_balance / avg_daily_expense)
autonomy_rate     = independent_income / total_expense
```

---

## 1. 系统要求

- Windows 10 / 11，或 macOS / Linux
- **Python 3.9+**（自带 sqlite3 模块）
- 浏览器：Chrome / Safari / Firefox 任意现代版本
- 首次安装运行组件时需要联网；日常使用无需联网、无需账号注册，所有数据本地存储

---

## 2. 一键启动

### Windows（推荐，不需要使用终端）

1. 打开项目文件夹，双击 **`启动我的生活中枢.cmd`**（旧启动器仍然兼容）。
2. 首次运行时，启动器会创建本地环境并安装必要组件；完成后自动打开浏览器。
3. 以后启动只需双击同一个文件。
4. 使用结束后双击 **`停止我的生活中枢.cmd`**，浏览器页面即可关闭。

如果电脑没有 Python 3.9 或更高版本，启动器会给出中文提示并打开 Python 官方下载页。安装 Python 时勾选 **Add Python to PATH**，安装完成后重新双击启动器即可。

浏览器没有自动打开时，手动访问 **http://127.0.0.1:8766**。

### macOS / Linux

解压压缩包后进入项目目录：

```bash
cd 财富自由指南灯
chmod +x run.sh         # 仅首次需要赋予执行权限
./run.sh
```

`run.sh` 会自动完成：

1. 创建 `.venv` 虚拟环境（仅首次）
2. 安装依赖：fastapi / uvicorn / pydantic
3. 启动后端服务 → 监听 `http://127.0.0.1:8766`
4. 自动打开浏览器

如自动打开失败，手动访问 → **http://127.0.0.1:8766**

停止服务：终端按 `Ctrl + C`。

---

## 3. 首次使用 · 三步走

### 步骤 ① 设置个人参数

首次进入会弹出引导对话框，依次填写：

| 字段 | 说明 | 示例 |
|------|------|------|
| **出生日期** | 用于计算剩余生命方格 | YYYY-MM-DD |
| **目标年龄** | 你希望活到的年龄（决定方格总数） | 80 |
| **显示过去** | 是否把出生至今的日子也画成方格 | 关闭 = 第一格是今天 |
| **计入起始可支配存款** | 是否把现在真正由你支配的钱计入生活续航 | 打开后填入金额 |
| **开始记账时的可支配存款** | 不含未来生活费承诺，也不含不能动用的资产 | ¥10,000 |
| **记账天数（手动覆盖）** | 0 = 自动从交易记录派生 | 60（强制 60 天） |
| **日均花销（手动覆盖）** | 0 = 自动派生 | 150（强制 ¥150/天） |

> **手动覆盖说明**：默认两项填 0 即可，让系统从你的真实交易数据派生。
> 仅在想做「如果我每天只花 ¥X，我能买到多少自由」的假设模拟时填具体值。

### 步骤 ② 记一笔账

主界面下方的输入区：

1. 选择类型：**收入** 或 **支出**
2. 收入需要选择来源：家庭生活费、奖助学金、兼职/实习、个人项目、投资或其他自主收入
3. 选择这笔钱进入或离开哪个真实账户
4. 支出时选择分类：餐饮、交通、学习、住宿、医疗、娱乐、社交、数字服务或其他
5. 输入金额（必须 > 0）
6. 选择日期（默认今天）并填写可选备注
7. 点击「记入生活支持」「点亮自主星光」或「记下花销」

提交后立即触发：

- **数据落盘**（SQLite，本地 `data/ledger.db`）
- **方格变化动画**：资金续航增加时点亮，支出造成续航下降时熄灭
- **统计卡片实时更新**：家庭支持 / 自主收入 / 总支出 / 日均花销
- **自主能力更新**：展示自主收入能够覆盖多少天，以及相对已记录支出的覆盖率

### 步骤 ③ 浏览人生方格

页面中央是 **Canvas 人生方格图**：

- 每个方格 = 你生命中的一天
- **银蓝色** = 家庭支持与起始存款形成的续航
- **金黄色** = 自主所得形成的续航
- **深灰色** = 未点亮（还要继续努力的日）
- **超额星光** = 当前可支配资金续航超过了剩余寿命（触发未来全覆盖彩蛋）

页面顶部显示：当前资金续航、自主生活覆盖率、支持资金留存和自主所得留存。

### 步骤 ④ 使用资金驾驶舱

右侧顶部切换到 **“资金驾驶舱”**，可以看到：

- 所有账户的实时余额与交易数量
- 本月家庭支持、自主收入、总支出和净现金流
- 本月支出分类条形图
- 最近六个月自主收入与支出趋势
- 新增银行卡、微信、支付宝、校园卡、现金等账户
- 在自己的账户之间转账，转账不会进入收入或支出统计
- 按手机银行、微信等显示的实际数字校准余额，系统只记录差额

升级前填写的起始可支配存款会自动迁入 **“日常资金”** 账户，历史交易也会归入该账户，不会重复计算。

### 步骤 ⑤ 使用规划中心

右侧顶部切换到 **“规划中心”**，可以设置：

- 每月预计收到的生活费金额和到账日
- 每月支出预算（填写 0 时改用真实账本花销估算）
- 一个或多个储蓄目标、目标金额、已预留金额和目标日期
- 预计月底余额、下次生活费到账前余额、目标已预留和未分配余额

规划中心严格与真实账本分离：预计生活费只参与预测，实际到账后仍需手动记一笔“家庭生活费”；目标的“已预留”只是对现有余额的用途标记，不会凭空增加资产。

### 步骤 ⑥ 设置分类预算预警

在 **规划中心 → 分类预算预警** 中，为餐饮、交通、学习、住宿等分类填写每月预算并保存：

- 未达到 80%：正常显示
- 达到预算的 80%：金色提醒
- 超过预算：红色提醒

分类预算只用于提醒，不会阻止记账，也不会改变账户余额。顶部“每月支出预算”用于总体预测，下面的分类预算用于看具体花到了哪里；两者可以独立设置。

### 步骤 ⑦ 导入 CSV 账单

进入 **规划中心 → CSV 账单导入**：

1. 可先下载空白模板，或准备 UTF-8 / GB18030 编码的 CSV
2. 选择“未填写账户时记入”的默认账户
3. 点击「分析并预览」
4. 检查每行日期、类型、金额、账户和错误提示
5. 全部行通过后，点击「确认导入 N 笔」

支持中英文表头：`日期/date`、`类型/type`、`金额/amount`、`分类/category`、`来源/source`、`备注/note`、`账户/account`。其中日期和金额必填；类型填写“收入”或“支出”。收入没有填写来源时，会保守归入“家庭生活费”，避免误增自主收入。

导入采用整批事务：任意一行有错误时一笔也不会写入；同一批规范化内容再次导入会返回重复提示。导入完成后，页面下方会出现批次记录，可点击「撤销整批」一次删除该批仍存在的账目。

### 步骤 ⑧ 使用固定支出日历

右侧顶部切换到 **“月度日历”**：

1. 填写固定账单名称、金额、每月到期日、支出分类和支付账户
2. 点击「添加固定账单」只会创建提醒，不会自动扣款
3. 账单进入三日内到期时会提醒；超过到期日仍未支付时显示逾期
4. 真正付款后点击「记为已支付」，确认后才生成一笔真实支出
5. 若操作错误，可点击「撤销支付」，对应支出会一起删除

删除未支付提醒不会影响账本；删除已经支付的提醒，只停止未来提醒，已经发生的真实支出会保留。若从“最近交易”直接删除固定账单生成的支出，对应月份也会自动恢复为未支付状态。

### 步骤 ⑨ 查看月度复盘

“月度日历”下方的复盘只使用已经记录的真实数据，展示：

- 本月收入、支出和净现金流
- 本月交易数量和相对上月的支出变化
- 最大支出分类及金额
- 有收入样本时计算的储蓄率
- 固定账单计划金额、已支付和待支付情况

没有足够数据时，系统会明确显示“暂无样本”，不会生成看似精确但没有依据的结论。

### 步骤 ⑩ 使用数据中心

右侧顶部切换到 **“数据中心”**，可以完成年度分析、全局搜索和备份恢复。

#### 年度报告

输入年份并点击「生成」，系统按十二个月汇总：

- 年度收入、支出、净现金流和储蓄率
- 家庭支持与自主收入分别合计
- 最佳现金流月份和最高支出月份
- 年度支出分类

点击「导出 CSV」会下载该年度的逐月汇总。报告只统计真实账目，不会把预计生活费或储蓄目标计入收入。

#### 全局交易搜索

可以组合使用备注或金额关键词、收入/支出类型、支出分类、账户和日期范围。搜索结果会同时显示匹配笔数、收入、支出和净额；重置后恢复显示全部交易。

#### 完整备份

点击「导出完整备份」会下载 JSON 文件，内容包括：

- 设置、账户、交易、转账和余额校准
- 生活费周期、学期预算、储蓄目标和分类预算
- CSV 导入批次
- 固定账单及其支付关联
- 健身、饮食、恢复、学习、待办、习惯与每日回顾

#### 安全恢复

1. 选择由本平台导出的 JSON
2. 核对页面显示的备份时间、账户数、交易数、目标数、固定账单数和余额
3. 输入确认词 **“恢复”**
4. 点击「覆盖当前数据并恢复」并再次确认

恢复前，系统会自动在 `data` 目录生成 `ledger-auto-before-restore-*.db`。文件格式不符、缺少账户或存在破损引用时，恢复会被拒绝，当前账本保持原样。

### 步骤 ⑪ 使用“今天”主页

平台默认打开 **“今天”**，把最常用的信息集中在一屏：

- 今天建议还能花多少、今天已经花多少
- 本月预算剩余与下次生活费日期
- 未来 7 天待处理的固定账单
- 最近的储蓄目标和本学期预算节奏

“今天建议还能花”只在已经设置月预算或学期预算时计算；两者都存在时取更谨慎的日额度。账户余额不会直接被当成今日可花。

在“一句话记账”中可以输入 `午饭 16.5 支付宝`、`昨天地铁 3 微信` 或 `奖学金 2000 银行卡`。系统会先显示类型、金额、账户、分类、来源和日期的可编辑预览。只有点击 **“确认入账”** 才会新增真实交易；点击取消或只查看预览不会改变账本。

### 步骤 ⑫ 设置大学生学期预算

进入 **规划中心 → 大学生学期预算**，填写学期开始日期、结束日期、学期总预算，并选择“在校”或“寒暑假”模式。

平台会根据学期内真实支出计算：

- 学期预算已使用比例和时间已过去比例
- 学期剩余预算与剩余天数
- 从今天起建议的学期日预算
- 消费速度是否明显快于学期时间进度

学期预算只是规划参数，不会新增收入、支出或改变账户余额。

---

## 4. 常用操作

### 4.1 删除一笔账

主界面右下「最近交易」列表 → 点击单笔右侧 × → 立即删除并重算。

### 4.2 修改个人设置

页面顶部齿轮图标 → 重新打开设置对话框 → 修改后点击「开始 / 保存」。

账户余额不再从设置面板修改。请在 **资金驾驶舱 → 我的账户** 中创建真实账户；新账户的“初始余额”表示开始使用平台时该账户已有的钱。

### 4.3 在账户之间转账

进入 **资金驾驶舱 → 账户操作 → 账户间转账**：

1. 选择转出和转入账户
2. 填写金额、日期和可选备注
3. 点击「确认转账」

转账只改变两个账户各自的余额，全部账户总额不变，也不会被算作收入、支出或本月净现金流。只有一个账户时，转账按钮会保持禁用；需要先新增另一个账户。

### 4.4 校准实际余额

当平台余额和手机银行、微信、支付宝或校园卡里的实际余额不一致时，进入 **资金驾驶舱 → 账户操作 → 校准实际余额**：

1. 选择账户
2. 输入外部账户当前显示的实际余额
3. 确认系统提示的差额后点击「校准余额」

平台只保存“平台余额与实际余额之间的差额”。校准不会伪造一笔收入或支出，因此不会扭曲月度现金流；差额会直接进入该账户的余额计算。若实际余额与平台一致，不会新增校准记录。

### 4.5 注入演示数据（仅供首次体验）

```bash
.venv/bin/python backend/seed_demo.py
```

默认参数：60 天 · 平均日花销 ≈ ¥70 · 平均日收入 ≈ ¥500。
**注意**：seed_demo 会**清空现有交易**，仅首次体验时使用。

### 4.6 自定义平均日花销的演示数据

编辑 `backend/seed_demo.py` 顶部：

```python
DAYS = 60                              # 跨度天数
TARGET_AVG_EXPENSE_PER_DAY = 150.0     # 改为你想要的日均
TARGET_AVG_INCOME_PER_DAY = 800.0
```

重新运行即可。

### 4.7 完全重置

```bash
rm data/ledger.db
./run.sh                # 重启会自动建空库
```

---

## 5. 文件结构

```
财富自由指南灯/
├── README.md              简介
├── 使用指南.md             本文件
├── 设计方案.md             数学模型 / API 契约 / 动画规格（开发者向）
├── run.sh                 一键启动脚本
├── backend/
│   ├── main.py            FastAPI + SQLite 后端（单文件）
│   ├── seed_demo.py       演示数据注入器
│   └── requirements.txt   Python 依赖
├── frontend/
│   ├── index.html         页面结构
│   ├── style.css          视觉设计
│   ├── app.js             状态机 / API 调用 / 仪式编排
│   └── grid.js            Canvas 方格 + 动画引擎
└── data/                  自动创建，存放 ledger.db
```

---

## 6. 隐私与数据

- **所有数据存储在本地** `data/ledger.db` 文件，**不上传任何服务器**
- 后端只监听 `127.0.0.1`（仅本机访问，局域网内不可见）
- 想换设备：直接复制整个项目目录（含 `data/ledger.db`），在新设备 `./run.sh`
- 想清空：删除 `data/ledger.db`

---

## 7. API 接口（可选 · 用于二次开发）

服务运行后访问 `http://127.0.0.1:8766/api/docs` 看 Swagger 文档。

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/state` | GET | 获取设置 + 统计 + 最近 50 笔交易 |
| `/api/settings` | POST | 更新设置 |
| `/api/transactions` | GET | 列出交易（支持 limit / offset） |
| `/api/transactions` | POST | 新增交易 |
| `/api/transactions/{id}` | DELETE | 删除指定交易 |
| `/api/accounts` | GET / POST | 获取账户或新增账户 |
| `/api/accounts/{id}/reconcile` | POST | 按实际余额记录校准差额 |
| `/api/transfers` | GET / POST | 获取或新增账户间转账 |
| `/api/transfers/{id}` | DELETE | 删除指定转账并恢复账户余额 |
| `/api/planning` | GET | 获取生活费周期、储蓄目标和预测 |
| `/api/planning/settings` | POST | 保存生活费周期和月度预算 |
| `/api/planning/semester` | POST | 保存学期起止日期、总预算和在校/假期模式 |
| `/api/today` | GET | 获取今日预算、提醒、目标与学期节奏聚合 |
| `/api/quick-entry/parse` | POST | 解析一句话记账并返回预览，不写入账本 |
| `/api/goals` | POST | 新增储蓄目标 |
| `/api/goals/{id}/progress` | POST | 更新目标已预留金额 |
| `/api/goals/{id}` | DELETE | 删除储蓄目标，不影响账户余额 |
| `/api/budgets/categories` | POST | 保存各支出分类的月预算 |
| `/api/import/transactions` | POST | 校验并整批导入账目，重复内容防重 |
| `/api/import/batches` | GET | 获取最近导入批次 |
| `/api/import/batches/{id}` | DELETE | 撤销整批导入并重算余额 |
| `/api/calendar` | GET | 获取固定支出日历、到期状态和月度复盘 |
| `/api/bills` | POST | 创建固定账单提醒，不生成交易 |
| `/api/bills/{id}` | DELETE | 停止固定账单提醒 |
| `/api/bills/{id}/pay` | POST | 确认支付并生成真实支出 |
| `/api/bills/{id}/payments/{month}` | DELETE | 撤销该月支付及对应支出 |
| `/api/search/transactions` | GET | 按关键词、类型、分类、账户和日期搜索交易 |
| `/api/reports/annual` | GET | 返回指定年份的十二个月年度报告 |
| `/api/backup/export` | GET | 导出完整版本化 JSON 快照 |
| `/api/backup/restore` | POST | 自动备份后原子恢复完整快照 |
| `/api/reflection` | GET | 按日期读取每日回顾、最近记录与该周快照 |
| `/api/reflections` | POST | 新增或更新某天的每日回顾 |
| `/api/reflections/{id}` | DELETE | 删除指定每日回顾 |
| `/api/life-calendar` | GET | 按月份和选中日期读取生活事实与安排的只读投影 |
| `/api/life-goals` | GET / POST | 获取或新增个人生活目标 |
| `/api/life-goals/{id}/status` | POST | 主动修改目标的进行、暂停或完成状态 |
| `/api/life-goals/{id}/milestones` | POST | 为生活目标添加里程碑 |
| `/api/goal-milestones/{id}/toggle` | POST | 切换里程碑完成状态 |
| `/api/life-search` | GET | 按关键词、来源模块和日期范围只读搜索全部生活记录 |
| `/api/life-timeline` | GET | 按来源模块、条目类型和日期范围分页浏览生活轨迹 |

---

## 8. 常见问题

**Q：启动报「Address already in use」？**
A：8766 端口被占用。运行 `lsof -i :8766` 找进程并 `kill`，或修改 `run.sh` 中的 `PORT`。

**Q：浏览器没有自动打开？**
A：手动访问 `http://127.0.0.1:8766` 即可。

**Q：能不能不用每次启动电脑？**
A：可以使用可选云端模式。仓库里的 `Dockerfile` 与 `render.yaml` 已准备好单用户部署；云端必须设置至少 12 个字符的访问密码，并使用持久磁盘。首次部署后，通过“数据中心 → 完整备份”把本地数据迁移过去。云端与本地不会自动双向合并，长期使用时只选其中一处作为主数据副本。

**Q：第一次进入没有引导弹窗？**
A：检查 `data/ledger.db` 是否已存在历史记录。删除该文件后重启 → 重新触发引导。

**Q：方格全黑没有点亮？**
A：因为净储蓄（收入 - 支出）≤ 0。多记一笔收入即可看到方格开始点亮。

**Q：能不能两人共用一个账本？**
A：当前为单用户单库设计。多人需各自部署一份独立实例。

**Q：手动覆盖记账天数 / 日均花销有什么意义？**
A：用于模拟假设场景。例如想知道「如果我每天只花 ¥100 维持 365 天，我已有的存款能买几年自由」，把覆盖值填进去即可即时看到效果。**填 0 表示按真实数据计算**。

---

## 9. 设计哲学

> 数据是真相，动画是仪式。仪式服从真相。

- 任何视觉效果都必须由数学模型驱动，绝不为了好看而作弊
- 简洁优先：函数只做一件事，能消除分支不增加 if/else
- 无后向兼容包袱：用户的真实需求 > 历史代码的延续性

详见同目录 **设计方案.md**。

---

祝你早日点亮所有方格。
