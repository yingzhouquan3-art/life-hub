# 我的生活中枢 · 使用指南

> 一个只给自己用的一体化生活平台。数据全部留在你自己的电脑上。
>
> 它的目标不是"记录得完整"，而是**让记录这件事便宜到你愿意一直做下去**。

---

## 目录

1. [这是什么](#1-这是什么)
2. [启动](#2-启动)
3. [第一天：三件事就够](#3-第一天三件事就够)
4. [每天：怎么把记录成本压到最低](#4-每天怎么把记录成本压到最低)
5. [在手机上用](#5-在手机上用)
6. [每周和每月](#6-每周和每月)
7. [模块速查](#7-模块速查)
8. [个人账本](#8-个人账本)
9. [数据安全：备份与恢复](#9-数据安全备份与恢复)
10. [这个平台刻意不做的事](#10-这个平台刻意不做的事)
11. [常见问题](#11-常见问题)
12. [给二次开发的接口](#12-给二次开发的接口)

---

## 1. 这是什么

左侧是模块栏，每个模块只管自己那摊事：

| 模块 | 管什么 |
|---|---|
| **总览** | 今天各方面的状态汇总在一页 |
| **日历** | 按日期看所有模块的记录与安排 |
| **目标** | 长期方向与阶段里程碑 |
| **账本** | 账户、收支、预算、账单、报表 |
| **健身** | 活动记录 + 动作库、组数、训练容量、个人纪录 |
| **身体** | 身体指标：体重、体脂、围度 |
| **饮食** | 每餐与饮水 |
| **恢复** | 睡眠、精力、心情 |
| **学习** | 专注时段 + 番茄钟 |
| **节奏** | 待办与每日习惯 |
| **复盘** | 每日回顾与周度快照 |
| **收集** | 还没想好归哪的随手记录 |
| **洞察** | 同期变化、数据健康度、标签 |

按 `Ctrl+K` 可以随时全局搜索，跨所有模块。

**一条贯穿全部设计的原则**：每个模块保存自己的原始事实，跨模块的页面（总览、日历、轨迹、搜索、洞察）**只读不写**。点进去编辑，永远回到那条记录所属的模块。

---

## 2. 启动

### Windows

双击 **`启动我的生活中枢.cmd`**，浏览器会自动打开。用完双击 **`停止我的生活中枢.cmd`**。

首次启动会自动准备运行环境，要等一会儿。没装 Python 的话启动器会打开下载页。

### macOS / Linux

```bash
chmod +x run.sh
./run.sh
```

访问 **http://127.0.0.1:8766**

---

## 3. 第一天：三件事就够

> **想先看看它长什么样？** 「账本 → 数据中心 → 示例数据」可以借你 60 天的
> 记录，覆盖账本、健身、学习、恢复、饮食、身体、习惯与待办，趋势和报表
> 立刻就有东西可看。它**不删除你已有的任何记录**，每写一条都登记在册，
> 一键就能精确撤走。看完再开始记你自己的。


不要一上来就把十几个模块都填满，那样第三天就会放弃。

### ① 设置个人参数

首次打开会引导你填出生日期和目标年龄——这是"人生方格"的计算基础。
如果有起始存款，勾上"使用初始资产"并填入金额。

### ② 记一笔真实的账

在「今天」页的输入框里打一句话，比如：

```
午饭 16.5 支付宝
```

点解析 → 出现可编辑预览 → 确认后才写入。**解析这一步不会写任何数据。**

### ③ 看一眼总览

回到「总览」，你会看到今天的状态。刚开始几乎全是 0，这是正常的——
那些 0 只表示"还没记"，不表示"没发生"。

**第一天到此为止。** 剩下的模块等你真的需要时再打开。

---

## 4. 每天：怎么把记录成本压到最低

这是整个平台最要紧的部分。记录只要超过十秒，就不会长期坚持。

### 键盘

| 键 | 做什么 |
|---|---|
| <kbd>N</kbd> | 从任何地方跳到「一句话记录」，光标直接落进输入框 |
| <kbd>/</kbd> | 打开全局搜索 |
| <kbd>Ctrl</kbd>+<kbd>K</kbd> | 同上 |
| <kbd>回车</kbd> | 在输入框里：解析这句话；解析完焦点会落到「确认」上，再按一次就记好了 |
| <kbd>Esc</kbd> | 收起预览、关掉搜索或正在编辑的行 |

正在输入框里打字时这些键不会被抢走，该打什么就打什么。

### 一句话记录

「今天」页顶部的输入框能识别六个模块，不只是记账：

| 你打的 | 它记到哪 |
|---|---|
| `午饭 16.5 支付宝` | 账本 · 支出 · 餐饮 |
| `跑步 30 分钟 强度 6` | 健身 · 有氧 |
| `午饭 鸡胸肉 450千卡` | 饮食 · 午餐 |
| `睡了 7 小时 精力 4` | 恢复 |
| `学习 高等数学 45 分钟` | 学习 |
| `记得 周五前交课程项目` | 待办 |

**记好之后屏幕下方会出现一条提示，带「撤销」**。一句话记录是靠猜决定落到
哪个模块的，猜错是常事（「午饭 16.5」会被判成饮食而不是账本），所以给了
一次后悔的机会，不用自己翻到那个模块去找那条记录。

删一笔账也一样：交易列表的删除**不弹确认框**（每次都确认只会让人养成闭眼
点「是」的习惯），删完给你一条带撤销的提示。撤销是照原样重记一笔——数据
回来了，但它是新的一条；如果原来属于某批导入，那层关系不会恢复，提示里
会写明。

判错了不要紧——预览上方有六个模块标签，**点一下就改判**，后端会按新归属重新解析同一句话（所以"午饭"改判到饮食后仍然认得出是午餐，不会退化成加餐）。

替你填的默认值会显式提示，比如"没有识别到时长，先按 30 分钟填入，请确认"。
认不出的句子它会直说认不出，不会硬塞进某个模块。

### 番茄钟

在「学习」页顶部。选好科目和时长，点开始。

**倒计时存在电脑上，不在网页里**——刷新页面、换手机看、合上笔记本再打开，剩下的时间都还是对的。

结束时：

- **确认结束并记录** → 按**实际专注的分钟数**写一条学习记录（不是计划时长）
- **结束但不记** → 只停掉，不写学习时长

不足一分钟不记。休息永远不计入学习时长。

### 待确认捕获

如果配好了手机的支付通知转发，抓到的付款会出现在「今天」页的"待确认捕获"里。

**它们在你确认之前不进入任何统计**——余额、月度、预算、报表都读不到。
选好分类点确认，才会变成一笔真正的交易。

分类通常已经预选好了——这是**商户分类记忆**：确认过一次"星巴克 → 餐饮"，下次同一商户自动预选。
旁边会注明"按「星巴克」预选"，让你知道这不是随便挑的。预选错了去「账本 → 数据中心 → 分类规则」改，见下。

> 同一笔付款如果同时被微信通知和银行短信抓到（金额相同、相差 60 秒内），会自动合并成一条，并保留两个来源。

### 收集箱

想到什么但还没想好归哪，扔进「收集」。之后再归档。

归档只是**标记这条去了哪个模块**，不会复制内容，也不会在那个模块凭空创建记录。

---

## 5. 在手机上用

**最简单的方式：手机和电脑连同一个 WiFi**，不需要装任何东西。

1. 双击 **`启动并允许手机访问.cmd`**（macOS / Linux 用 `LIFE_HUB_HOST=auto ./run.sh`）
2. 配对页面会自动打开，复制那条带 token 的地址
3. 发到手机上打开（微信发给自己最快）

手机端做四件事：

- **一句话记录**：和电脑上同一个解析器，认得六个模块
- **今天要做的**：每日习惯打卡、今天的待办、逾期的待办，点一下就完成。
  习惯旁边显示连续多少天
- **待确认捕获**：手机抓到的付款，确认后才进账本

记完之后顶部会出现一条带「撤销」的提示。手机上改不了已有记录，猜错模块
的话没有撤销就得回到电脑前，所以这里比电脑更需要它。
- **今天记了什么**：一屏汇总

报表、趋势、复盘、导入留在电脑上——手机只解决「来不及记」和「人不在电脑前」。

> **离线点的那一下不会丢，也不会反着来。** 没网时操作排进本地队列，
> 联网后自动补发。排的是「设成已完成」而不是「切换一下」——否则你在没网的
> 地方打了卡、回家又在电脑上打了一次，那条迟到的补发会把卡**取消掉**，
> 而你完全看不出来。

### 连不上时

打开 `http://127.0.0.1:8766/pair.html`，页面上「**连不上？先看这里**」会逐条检查并告诉你卡在哪。手机访问要同时满足三件事，缺一条现象都是"网页打不开"：

| 关卡 | 常见原因 |
|---|---|
| 后端监听在局域网地址 | 用了旧启动器，只监听本机 |
| 防火墙放行 8766 端口 | **Windows 默认拦截所有入站连接**，需要手动加规则 |
| 手机和电脑在同一网络 | 学校和公共 WiFi 常开客户端隔离，同网设备互相看不见 |

放行端口：双击 **`添加防火墙规则.cmd`**，在弹出的系统提示里点「是」。
它只加一条限定在"专用网络"的规则，并问你要不要把当前网络改成专用；不会关防火墙，也不动其它规则。

第三条查不出来。前两条都通过还连不上，多半就是它——**用手机开热点让电脑连上去**即可。

### 关于"安装成应用"和离线

浏览器只把 https 和 localhost 当成安全来源，而手机走的是 `http://192.168.x.x`。所以：

- **没有「安装应用」按钮**，也**没有离线外壳**——页面没打开时是打不开的
- 浏览器菜单里的「添加到主屏幕」仍然能用，但那只是书签快捷方式，点开仍带地址栏
- 离线时**已经打开着的页面照样能记**，记录进本地队列，联网后自动补发

---

## 5.5 让它主动找你（可选）

平台默认从不打扰你——所有东西都要你先想起来打开它。对习惯打卡来说，
这基本就是全部胜负手。

双击 **`添加每日提醒.cmd`**，填一个时间（比如 21:30），就设好了。
不需要管理员权限，只在你自己的计划任务里加一条。取消就双击
**`移除每日提醒.cmd`**。

它有意做得很克制：

- **只在你当天什么都没记的时候才出声。** 记过了就闭嘴——一个天天准点响的
  提醒，两周之内就会被无视，之后真正该看的提醒也一起被忽略了
- **服务没在运行时不弹。** 那多半说明你不在电脑前，弹给空房间看没有意义
- 笔记本用电池、或者到点时正在休眠，都不会漏——错过了会补一次

设置完会**当场发一条测试通知**——通知这种东西「设置成功」不等于「你看得到」，
Windows 会因为专注助手或通知权限静默丢掉，而脚本这边完全无感。当场发一条，
你现在就能确认，而不是某天晚上才发现它一直没响过。

没看到那条测试通知的话：打开「设置 → 系统 → 通知」，确认通知总开关是开的，
并且「专注助手 / 请勿打扰」没开着。

> 只能提醒到**电脑**。手机收不到：这是台本地服务，没有公网入口，
> 安卓还要常驻后台才行。这条限制没法绕过。

---

## 6. 每周和每月

### 每周：复盘

「复盘」里写一条当天的回顾（亮点、困难、感谢、随便写，任选一项即可），
并能看到周一到周日各模块的事实快照。

快照里的 0 只表示那个模块这周没有记录，**不能推导现实中没有发生**。

### 每月：把导出的文件丢进「导入」

「账本 → 数据中心 → 导入」是**唯一的导入口**，不用先想清楚"我这个文件算哪一类"。
选中文件之后它会先告诉你：

> 这看起来是**微信支付账单**，确认后会写进「个人账本」
> · 账单解析器认出了这是它能处理的格式
> · 文件开头出现「微信支付」字样
> · 表头里有「收/支」

**依据是写给你复核的**——认错了就在旁边改选，不用换页面重来。认不出来时它
直说认不出，不会硬套一个类型把体重数据写进账本。

目前认得四种：微信账单、支付宝账单、运动 App 的训练记录、体重 / 体脂记录，
分别写进个人账本、个人健身和身体数据。

**文件格式**：微信和支付宝现在导出的是 **Excel（.xlsx）**，直接丢进来就行，
不用先另存成 CSV。CSV 当然也能用。

两种读不了，界面上会直接告诉你怎么办：

| 格式 | 怎么办 |
|---|---|
| **PDF** | PDF 里的表格是画上去的，取不回可靠金额和日期。在微信里重新导出时选 Excel |
| **老版 .xls** | 用 Excel 或 WPS 打开，另存为「Excel 工作簿 (.xlsx)」 |

认不出来的时候它会把**实际读到的前几行**摊出来给你看——是表头不对、还是
整份文件读空了，一眼能分辨。

点「看看会写入什么」之后是预览，**这一步不写任何数据**：

- **将写入**：库里没有的
- **已经记过**：日期金额都对得上的，不会重复
- **需要你判断**：方向不明的（余额宝转入、花呗还款、退款这些），不会自动写入
- **已跳过**：整笔退款、交易关闭、支付失败——这些不是实际支出

账单还会**逐行猜好支出分类**并注明依据（"按「星巴克」"）。猜不出来的老实落进
「其他」。每一行都能当场改，改完再确认。

> **导入不会把这些猜测变成规则。** 一次误判在几百行上自我强化，正是分类记忆
> 最该避免的事——规则只在你逐条确认捕获时才学。要改规则去「分类规则」。

同一份文件重复导入不会让记录翻倍，写错了可以整批撤销。
支付宝导出是 GBK 编码，程序会自动识别，不用你处理。

### 顺手看看洞察

「洞察」有四块：

- **趋势**：每个指标最近几周（或几个月）怎么走。有两条规矩值得知道：
  没记录的那期画成空白而不是 0（「没记」和「是 0」是两回事）；
  **变化按有记录那些天的日均算，不按总和**——上周记 2 天、这周记 6 天，
  总和自然翻三倍，那说明你记得更勤，不说明你花得更多。一期记录不满 3 天
  就直接说「暂不比较」，不给一个看起来很确定的百分比
- **同期变化**：两个指标是不是一起动。**这里全部是相关，不是因果**——配对天数少于 7 天时它会拒绝给数字，并说明为什么
- **数据健康度**：哪些指标最近没在记。它衡量的是模块还在不在被用，**不是自律程度**
- **标签**：一个 `#考研` 可以同时贴在学习记录、待办和支出上

---

## 7. 模块速查

每个模块都写清了"不记什么"，这比"记什么"更重要。

| 模块 | 记什么 | **不**记什么 |
|---|---|---|
| 健身 | 已完成的活动、组数×重量×次数 | 训练计划、医疗判断 |
| 身体 | 体重、体脂、围度（都可留空） | 健康评价；留空是"没量"不是 0 |
| 饮食 | 餐次、饮水、可选营养数值 | 精确营养诊断 |
| 恢复 | 睡眠、精力、心情 | 医疗结论 |
| 学习 | 已完成的专注时段 | 成绩、掌握程度 |
| 节奏 | 待办与习惯打卡 | 逾期不代表失败；未打卡不代表没做 |
| 目标 | 生活方向与里程碑 | 带金额的储蓄目标在账本里 |

**个人纪录**（健身）只描述"已经记录过的最好一次"，不代表能力上限。一段时间没刷新也不说明退步，可能只是没练那个动作。

**估算 1RM** 是按次数和重量推算的，不是实测，次数越多误差越大。

---

## 8. 个人账本

它不把所有入账笼统叫作"财富自由"：

- 家庭生活费和起始存款 → **蓝色方格**（支持资金续航）
- 奖学金、兼职、实习、项目、投资 → **金色方格**（自主所得）
- 深灰方格 → 当前资金还覆盖不到的未来日子

```
avg_daily_expense = total_expense / tracking_days
current_balance   = Σ 各账户余额
runway_days       = floor(current_balance / avg_daily_expense)
autonomy_rate     = independent_income / total_expense
```

### 常用操作

| 想做的事 | 在哪 |
|---|---|
| 账户之间转钱 | 数据中心 → 转账。**不会重复计成收入或支出** |
| 实际余额对不上 | 账户 → 余额校准。**只记差额，不伪造一笔消费** |
| 设分类预算 | 规划中心。达到 80% 提醒，超了标红 |
| 固定支出 / 订阅 | 规划中心 → 支持月付/季付/年付；非月付只在到期那个月提醒，不会月月骚扰 |
| 学期预算 | 规划中心 → 按在校/假期用不同消费节奏 |
| 改一笔记错的账 | 交易列表右侧的铅笔图标，就地改金额、日期、分类、备注 |
| 删一笔账 | 交易列表右侧的删除 |
| 分类老是预选错 | 数据中心 → 分类规则。改判或删掉那条规则 |
| 导入账单 / 运动数据 | 数据中心 → 导入。丢文件进去，它自己认 |

### 分类规则

「数据中心 → 分类规则」是预选分类的来源。开箱有 56 条常见商户当起点，
你每确认一笔捕获，它就多学一条。

- **改判**：填上同一个关键字、选新分类、点添加，那条规则被改写，不会变成两条打架的规则
- **删除**：删掉之后不再预选。删掉内置规则它不会自己回来——除非你把规则**全部**删光，那样下次启动会重新播种
- 关键字多的时候用右边的筛选框，关键字和分类名都能搜

两条边界值得记住：规则**只影响下拉框的默认值**，不会自动记账，也不会改动已经记好的账；
多条规则同时命中时取**最长**的那条，所以「星巴克」压得过「咖啡」。

### 订阅总览

规划中心顶部有一张订阅总览表，回答"我到底订了多少东西、一年花多少"。

- 按**离下次扣费还有几天**排序，7 天内的会标黄
- 非月付的会换算出**月均成本**——那是个换算值，不是某个月真的会扣的金额
- 表里的条目就是下方「固定支出」里的账单，增删会立刻反映到这张表

> 由固定账单「记为已支付」生成的那笔交易改不了——它同时被账单日历按月
> 引用着，改了两边会对不上。要改就先在日历里撤销那次支付，再重新记一笔。
> 界面上会直接这么告诉你。

**规划不改账**：规划里填的"预计生活费"不会自动变成一笔收入，储蓄目标预留也不会增加账户余额。

---

## 9. 数据安全：备份与恢复

所有数据都在 `data/ledger.db` 这一个文件里。

### 自动快照

每次启动会先做一次数据库自检，再自动留一份快照到 `data/snapshots/`：

- 默认每 24 小时一份，保留 30 天，且**不少于 10 份**
- 用 SQLite 自己的 backup 接口生成，写入过程中也能拿到一致副本
- 自检发现问题时**跳过备份**，避免用可能损坏的内容顶掉旧快照

「账本 → 数据中心 → 本地备份」能看到"上次备份多久之前"，超过两天会变黄，也可以随时手动留一份。

### 但这挡不住硬盘坏掉

**快照和数据库在同一块硬盘上。** 重要阶段请用「完整备份 → 导出」存一份 JSON 到别的地方（U 盘、网盘、另一台电脑）。

### 恢复

「数据中心 → 完整备份 → 恢复」，先选一个本平台导出的 JSON，再在输入框里手打「恢复」两个字，按钮才会亮。
恢复前会自动把当前数据库整份另存为回退文件，所以恢复错了也能退回来。

---

## 10. 这个平台刻意不做的事

这些不是漏做，是明确的设计决定：

- **不评价你。** 没有评分、没有排行、没有连续中断的惩罚。习惯没打卡只表示没有记录，不表示你没做。
- **不把"没记录"当成"没发生"。** 所有的 0 和空白都只代表未知。
- **不宣称因果。** 洞察里只呈现"两个指标一起动过"，绝不说成"因为睡得好所以学得多"。
- **不替你编造事实。** 自动捕获抓到的付款在你确认前不进任何统计；解析不出金额时宁可什么都不写，也不猜一个数。
- **不给医疗、营养或心理建议。** 身体和恢复模块只记录，不下结论。
- **不上传任何数据。** 后端默认只监听本机；开手机访问时只额外监听你自己的局域网地址，启动脚本明确拒绝监听 `0.0.0.0`。

---

## 11. 常见问题

**Q：启动报「文件正由另一进程使用」？**
A：上一个服务还在跑。双击 `停止我的生活中枢.cmd`；如果启动记录丢了，它会按端口找到那个进程并弹窗问你要不要结束。

**Q：8766 端口被占用？**
A：macOS / Linux 用 `lsof -i :8766` 找到进程再 kill；Windows 用上面那个停止脚本。

**Q：浏览器没自动打开？**
A：手动访问 `http://127.0.0.1:8766`。

**Q：方格全黑没点亮？**
A：净储蓄（收入 − 支出）≤ 0。多记一笔收入就会开始点亮。

**Q：手机连不上？**
A：见 [第 5 节](#连不上时)，配对页有逐条自查。

**Q：能不能不用每次开电脑？**
A：可以用可选的云端模式。仓库里的 `Dockerfile` 与 `render.yaml` 已准备好单用户部署，云端必须设置至少 12 个字符的访问密码并挂持久磁盘。**云端和本地不会自动双向合并**，长期只选一处作为主副本。

**Q：能两个人共用吗？**
A：单用户单库设计。多人各自部署独立实例。

**Q：手动覆盖记账天数 / 日均花销有什么用？**
A：模拟假设场景。比如"如果我每天只花 ¥100，现有存款能撑多久"。**填 0 表示按真实数据算。**

**Q：想从头再来？**
A：删掉 `data/ledger.db` 后重启。`data/snapshots/` 里还有历史快照，别一起删。

---

## 12. 给二次开发的接口

后端是 FastAPI，交互式文档在 **http://127.0.0.1:8766/api/docs**。

分层与依赖方向是单向的：

```
api/ → views/ → modules/ → core/
```

模块之间不互相调用，跨模块的汇总一律走 `views`（只读）。

**新增一个生活模块只需三步**：写 `modules/<name>.py` 并声明 `MODULE`（表、建表语句、备份列、清空顺序），在 `modules/__init__.py` 的 `MODULES` 里登记，写 `api/<name>.py` 并在 `main.py` 装配。建表、备份和恢复会自动覆盖到。

跑测试：

```bash
.venv/Scripts/python -m unittest discover -s tests -t .
```

设计取舍与领域约定见 **[CONTEXT.md](./CONTEXT.md)** 和 **[docs/adr/](./docs/adr/)**。

---

> 数据是真相，动画是仪式。仪式服从真相。
