# ADR-0004: 本地优先、可选单宿主云端

## 状态

Accepted

平台继续默认在本地运行；需要随时访问时，可以把同一套应用部署为带密码登录、HTTPS 和持久磁盘的单用户云端实例。我们不做本地 SQLite 与云端 SQLite 的双向同步，因为同时写入后的冲突、删除和恢复很难在不虚构生活事实的前提下自动合并；用户在一个时期选择一处主数据副本，通过完整备份迁移。云端入口与本地局域网令牌分开，只有显式云端模式且配置足够长的密码时才允许监听容器公网入口。
