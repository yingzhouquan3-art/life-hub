# 我的生活中枢 · 个人账本模块设计方案

> **核心原则**：家庭支持与自主所得必须分开呈现；生活费是支持，不冒充自主收入。

---

## 1. 第一性原理

传统记账只回答“我今天花了多少 / 收到了多少钱”。
本系统回答三个问题：

> 当前资金能续航多久？其中哪些来自家庭，哪些由自己创造？自主收入覆盖了多少支出？

银蓝色方格表示支持资金续航，金色方格表示自主所得续航。二者共同构成当前可支配余额，但不混淆来源。

---

## 2. 核心数学模型

### 2.1 定义

- `tracking_start_date`：开始记账的第一天（首笔交易日期）
- `today`：当前日期
- `tracking_days = (today - tracking_start_date).days + 1` （含起点 +1，避免 0 除）
- `family_support_income = Σ family_support`
- `independent_income = Σ scholarship + part_time + project + investment + other`
- `total_expense = Σ expense`
- `account_balance = opening_balance + account_income - account_expense + transfer_in - transfer_out + balance_adjustments`
- `current_balance = Σ account_balance`
- `avg_daily_expense = total_expense / tracking_days`
- `support_pool = initial_assets + family_support_income`
- `current_balance = max(support_pool + independent_income - total_expense, 0)`
- `runway_days = floor(current_balance / avg_daily_expense)`
- `autonomy_coverage_rate = independent_income / total_expense`

### 2.2 方格总数（人生剩余日方格）

- `birth_date`：用户生日
- `target_age = 80`（可调）
- `total_cells = (birth_date + target_age年 - today).days`

例：某用户 26 岁，剩余 ≈ 54×365 ≈ 19,710 格。

### 2.3 状态映射

| 方格状态 | 含义 | 数量 |
|---------|------|------|
| 🔵 support | 家庭支持 / 起始存款形成的续航 | 支持资金留存折算天数 |
| 🟡 independent | 自主所得形成的续航 | 自主所得留存折算天数 |
| ⚫ unlit | 未点亮 = 还要继续努力的日子 | `total_cells - lit` |
| ✨ overflow | 当前资金续航超过剩余人生 | `runway_days - total_cells` |

### 2.4 增量动画规则

每次新增交易后重新算 `freedom_days_bought_new` 与 `lit_count_new`：

- **新增收入 → 净增 N 格**：动画"点亮"末尾 N 格（从当前最后一格之后开始）
- **新增支出 → 抬高 avg → 净减 M 格**：动画"熄灭"末尾 M 格（从最后已点亮格往回熄）
- **支出但不引起熄灭**：仅刷新统计，无方格动画
- **特殊**：极端情况下 N 或 M 跨越 overflow 边界，按段分别处理

---

## 3. 数据模型 (SQLite)

```sql
CREATE TABLE settings (
  id INTEGER PRIMARY KEY CHECK (id = 1),
  birth_date TEXT NOT NULL,        -- 'YYYY-MM-DD'
  target_age INTEGER NOT NULL DEFAULT 80,
  currency TEXT DEFAULT 'CNY',
  created_at TEXT NOT NULL
);

CREATE TABLE transactions (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  occurred_on TEXT NOT NULL,       -- 'YYYY-MM-DD'
  type TEXT NOT NULL,              -- 'income' | 'expense'
  source TEXT NOT NULL,            -- family_support | scholarship | part_time | project | investment | other | expense
  category TEXT NOT NULL,          -- food | transport | study | housing | medical | entertainment | social | digital | other | income
  account_id INTEGER,
  amount REAL NOT NULL CHECK (amount > 0),
  note TEXT DEFAULT '',
  created_at TEXT NOT NULL
);

CREATE TABLE accounts (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  name TEXT NOT NULL,
  type TEXT NOT NULL,              -- bank | wechat | alipay | campus | cash | other
  opening_balance REAL NOT NULL DEFAULT 0,
  is_active INTEGER NOT NULL DEFAULT 1,
  created_at TEXT NOT NULL
);

CREATE TABLE account_transfers (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  from_account_id INTEGER NOT NULL,
  to_account_id INTEGER NOT NULL,
  amount REAL NOT NULL CHECK (amount > 0),
  occurred_on TEXT NOT NULL,
  note TEXT DEFAULT '',
  created_at TEXT NOT NULL,
  CHECK (from_account_id != to_account_id)
);

CREATE TABLE account_adjustments (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  account_id INTEGER NOT NULL,
  delta REAL NOT NULL CHECK (delta != 0),
  actual_balance REAL NOT NULL,
  occurred_on TEXT NOT NULL,
  note TEXT DEFAULT '',
  created_at TEXT NOT NULL
);

CREATE TABLE planning_settings (
  id INTEGER PRIMARY KEY CHECK (id = 1),
  monthly_allowance_amount REAL NOT NULL DEFAULT 0,
  allowance_day INTEGER NOT NULL DEFAULT 1,
  monthly_spending_budget REAL NOT NULL DEFAULT 0,
  updated_at TEXT NOT NULL
);

CREATE TABLE semester_settings (
  id INTEGER PRIMARY KEY CHECK (id = 1),
  start_date TEXT,
  end_date TEXT,
  total_budget REAL NOT NULL DEFAULT 0,
  mode TEXT NOT NULL DEFAULT 'in_school', -- in_school | vacation
  updated_at TEXT NOT NULL
);

CREATE TABLE savings_goals (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  name TEXT NOT NULL,
  target_amount REAL NOT NULL,
  saved_amount REAL NOT NULL DEFAULT 0,
  target_date TEXT,
  is_active INTEGER NOT NULL DEFAULT 1,
  created_at TEXT NOT NULL
);

CREATE TABLE category_budgets (
  category TEXT PRIMARY KEY,
  amount REAL NOT NULL,
  updated_at TEXT NOT NULL
);

CREATE TABLE import_batches (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  filename TEXT NOT NULL,
  content_hash TEXT NOT NULL UNIQUE,
  row_count INTEGER NOT NULL,
  created_at TEXT NOT NULL
);

CREATE TABLE recurring_bills (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  name TEXT NOT NULL,
  amount REAL NOT NULL,
  day_of_month INTEGER NOT NULL,
  category TEXT NOT NULL,
  account_id INTEGER NOT NULL,
  note TEXT DEFAULT '',
  is_active INTEGER NOT NULL DEFAULT 1,
  created_at TEXT NOT NULL
);

CREATE TABLE recurring_bill_payments (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  bill_id INTEGER NOT NULL,
  month TEXT NOT NULL,
  transaction_id INTEGER,
  paid_on TEXT NOT NULL,
  created_at TEXT NOT NULL,
  UNIQUE (bill_id, month)
);

-- transactions 另增 import_batch_id / import_row_number，支持整批撤销。
CREATE INDEX idx_tx_date ON transactions(occurred_on);
```

---

## 4. 后端 API

技术栈：**Python 3 + FastAPI + sqlite3 (stdlib) + uvicorn**

| Method | Path | 说明 |
|--------|------|------|
| GET    | `/api/state` | 返回完整状态（settings + stats + 最近交易 + lit_count） |
| GET    | `/api/dashboard` | 返回账户余额、本月分类统计和近六个月趋势 |
| GET    | `/api/accounts` | 返回活动账户及实时余额 |
| POST   | `/api/accounts` | 新增账户和初始余额 |
| GET    | `/api/transfers` | 返回最近账户间转账 |
| POST   | `/api/transfers` | 在两个活动账户之间转账；不计入收支 |
| DELETE | `/api/transfers/{id}` | 删除转账并恢复两个账户余额 |
| POST   | `/api/accounts/{id}/reconcile` | 按实际余额记录校准差额；不计入现金流 |
| GET    | `/api/planning` | 返回生活费周期、储蓄目标和月底预测 |
| POST   | `/api/planning/settings` | 保存预计生活费、到账日与月度预算 |
| POST   | `/api/planning/semester` | 保存学期范围、总预算和在校/假期模式 |
| GET    | `/api/today` | 聚合今日额度、账单、目标、生活费和学期节奏 |
| POST   | `/api/quick-entry/parse` | 确定性解析自然语言记账；只返回预览，不写入账本 |
| POST   | `/api/goals` | 新增储蓄目标；不改变真实余额 |
| POST   | `/api/goals/{id}/progress` | 更新目标已预留金额 |
| DELETE | `/api/goals/{id}` | 停用储蓄目标；不改变真实余额 |
| POST   | `/api/budgets/categories` | 保存分类预算并返回本月预算状态 |
| POST   | `/api/import/transactions` | 原子批量导入；内容哈希防止重复导入 |
| GET    | `/api/import/batches` | 返回最近导入批次和剩余账目数 |
| DELETE | `/api/import/batches/{id}` | 删除批次所属账目并允许重新导入 |
| GET    | `/api/calendar` | 返回固定账单日历、到期提醒和月度复盘 |
| POST   | `/api/bills` | 创建固定账单；不改变真实账本 |
| DELETE | `/api/bills/{id}` | 停用未来提醒；保留已经发生的支出 |
| POST   | `/api/bills/{id}/pay` | 每月幂等地生成一笔真实支出 |
| DELETE | `/api/bills/{id}/payments/{month}` | 撤销支付并删除关联支出 |
| GET    | `/api/search/transactions` | 组合条件搜索并返回匹配汇总 |
| GET    | `/api/reports/annual` | 返回年度总览、月度序列和分类统计 |
| GET    | `/api/backup/export` | 导出 12 张业务表的版本化 JSON 快照 |
| POST   | `/api/backup/restore` | 覆盖前自动备份，事务内恢复并检查引用完整性 |
| POST   | `/api/settings` | 首次设置 / 修改 birth_date target_age |

### 4.1 快照恢复安全约束

- 快照必须声明 `format = wealth-lighthouse-snapshot` 和受支持的版本号
- 必须包含所有核心业务表，且至少包含一个账户；旧版快照可缺少新增的 `semester_settings`
- 恢复按钮需要前端中文确认词，后端还要求固定确认令牌
- 覆盖前使用 SQLite 在线备份生成独立 `.db` 回退文件
- 删除与插入按外键顺序执行，任一错误整批回滚
- 恢复完成前运行 `PRAGMA foreign_key_check` 和交易账户孤儿检查
| POST   | `/api/transactions` | 新增交易，**返回**：前后 lit_count、delta、动画类型 |
| GET    | `/api/transactions` | 分页交易列表 |
| DELETE | `/api/transactions/{id}` | 删除（也返回 delta，触发反向动画） |

`POST /api/transactions` 响应：

```json
{
  "transaction": {...},
  "stats": {
    "total_income": 12000,
    "total_expense": 4500,
    "tracking_days": 90,
    "avg_daily_expense": 50.0,
    "freedom_days_bought": 240,
    "lit_count_before": 210,
    "lit_count_after": 240,
    "delta": 30,
    "animation": "light_up"   // 'light_up' | 'extinguish' | 'none'
  }
}
```

前端拿到 delta + 类型即可启动仪式动画。

---

## 5. 前端架构

**纯静态 HTML/CSS/JS**，Canvas 渲染方格（万级元素 DOM 会卡）。

### 5.1 布局

```
┌─────────────────────┬───────────────────────────────────────────────┐
│                     │                                                │
│   左 1/3            │   右 2/3                                       │
│   ─────────         │   ─────────                                    │
│   📒 账本面板         │   ✨ 人生方格视图（Canvas）                       │
│                     │                                                │
│  · 生活续航 / 自主覆盖  │     [年份索引: 26 27 28 29 ... 80]              │
│  · 设置（生日/目标）   │     ┌────────────────────────────────────┐    │
│  · 添加交易表单        │     │ 方格 grid (按年分行/分段)            │    │
│  · 最近交易列表        │     │                                    │    │
│  · 统计仪表盘          │     │  🟡🟡🟡🟡🟡⚫⚫⚫⚫⚫⚫⚫⚫⚫⚫           │    │
│                     │     │  ⚫⚫⚫⚫⚫⚫⚫⚫⚫⚫⚫⚫⚫⚫⚫            │    │
│                     │     └────────────────────────────────────┘    │
│                     │                                                │
└─────────────────────┴───────────────────────────────────────────────┘
```

### 5.2 视觉风格

- 主色：深邃黑底（`#0a0a0f`）+ 金色光源（`#ffd166` 点亮 / `#3a3a4a` 未亮）
- 字体：等宽 + 衬线混搭（账本数字用等宽显严肃，标题用衬线显仪式感）
- 整体氛围：**夜空中点亮星辰** —— 不是 dashboard，是 ritual。

### 5.3 方格 Canvas 规格

- 自适应宽度，每行 N 格（按年自动分行：一年一行，每行 365/366 格）
- 单格尺寸：4-8px（自适应屏宽 + 总格数）
- 渲染：高 DPI（`devicePixelRatio` 适配）
- 性能：仅在状态变化时重绘；动画期间用 RAF + 离屏 buffer

### 5.4 仪式动画核心（**项目灵魂**）

#### 点亮动画 `light_up`

1. **阶段 A · Zoom In (600ms)**：camera 平滑缩放 + 平移到第一格目标位置（`lit_count_before + 1`），目标 scale = 4-6x
2. **阶段 B · Sequential Ignite**：每格点亮间隔自适应：
   - delta ≤ 10：每格 180ms（充分仪式感）
   - delta ≤ 50：每格 80ms
   - delta ≤ 500：每格 25ms
   - delta > 500：每格 8ms（保持流畅）
3. **单格点亮微动画 (300ms)**：
   - 起：暗灰 → 黄白爆发（亮度 200%）
   - 中：橙黄光晕扩散（半径 4x）
   - 末：稳定金色 + 微弱呼吸光
4. **阶段 C · Zoom Out (800ms, ease-out)**：平滑回到全局视图

#### 熄灭动画 `extinguish`

1. **阶段 A · Zoom In (600ms)**：camera 缩放到最后一格已点亮位置
2. **阶段 B · Sequential Extinguish**（从尾往头熄）：
   - 单格熄灭微动画 (400ms)：金色 → 红橙闪烁一次 → 灰烬色 → 黑暗
   - 间隔同点亮节奏，但放慢 1.3x（失去比获得更慢）
3. **阶段 C · Zoom Out**

#### 未来资金全覆盖 ✨

- 当 `lit_count_after >= total_cells` 触发**全局烟花动画**：所有方格连锁波纹 + 中央巨型金光 + "FREE" 字样浮现
- 一次性彩蛋

---

## 6. 文件结构

```
财富自由指南灯/
├── 设计方案.md            ← 本文件
├── README.md             ← 启动说明
├── backend/
│   ├── main.py           ← FastAPI 应用 + SQLite
│   └── requirements.txt  ← fastapi, uvicorn
├── frontend/
│   ├── index.html
│   ├── style.css
│   ├── app.js            ← API client + 主控
│   └── grid.js           ← Canvas 方格 + 动画引擎
├── data/
│   └── ledger.db         ← SQLite 数据库（git ignore）
└── run.sh                ← 一键启动脚本
```

---

## 7. 启动方式

```bash
cd 财富自由指南灯
./run.sh        # 启动 FastAPI on :8765, 自动开浏览器
```

---

## 8. 实现优先级 (MVP)

- [x] 设计方案落盘
- [ ] P0：后端 API + 数据库
- [ ] P0：前端布局 + 账本面板
- [ ] P0：Canvas 方格渲染（静态）
- [ ] P0：点亮 / 熄灭 仪式动画
- [x] P1：未来资金全覆盖彩蛋
- [ ] P1：交易删除 / 编辑
- [ ] P2：导出 / 多账本

MVP 范围：P0 全部。

---

## 9. 关键不变量（写代码时反复对照）

1. `avg_daily_expense` 永远 = `total_expense / tracking_days`，不做平滑
2. `lit_count` 永远 = `floor((total_income - total_expense) / avg_daily_expense)`，幂等
3. 动画只是 lit_count 变化的**视觉投影**，不持久化任何中间状态
4. 删除交易 = 重算 + 反向动画（点亮变熄灭，反之亦然）

> 数据是真相，动画是仪式。仪式服从真相。
